apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: data-pump-integration-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: datapump_test
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "datapump_test"]
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: data-pump-integration-test
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
